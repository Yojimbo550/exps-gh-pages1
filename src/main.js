import { createApp } from 'vue'
import App from './App.vue'
import './registerServiceWorker'
import Pagination from 'v-pagination-3';
// createApp(App).component('pagination', Pagination)
createApp(App).mount('#app')


